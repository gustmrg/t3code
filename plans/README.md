# Implementation Plans

Generated by the improve skill on 2026-08-08. Execute in the order below unless
dependencies say otherwise. Each executor must read the plan fully before
starting, honor its STOP conditions, and update its row when done.

## Execution order & status

| Plan | Title                                                                                    | Priority | Effort | Depends on            | Status                                         |
| ---- | ---------------------------------------------------------------------------------------- | -------- | ------ | --------------------- | ---------------------------------------------- |
| 001  | Add Kimi Code as a first-party provider                                                  | P1       | L      | —                     | DONE                                           |
| 002  | Add Kimi thinking controls to the composer                                               | P1       | M      | 001                   | DONE                                           |
| 003  | [Consolidar terminal principal e preparar Chat](003-terminal-first-session-workspace.md) | P1       | L      | Branch terminal-first | DONE — automatizado; validação visual pendente |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale — finding fixed independently or approach
abandoned)

## Dependency notes

- Plan 001 is self-contained. Its phases are ordered so contracts land before
  server registration, and server behavior lands before client presentation.
- Plan 002 builds on the generic option-descriptor pipeline delivered by plan
  001 and must preserve Kimi's model-dependent ACP effort vocabulary.

## Findings considered and rejected

- Using `kimi provider list --json` for model discovery was rejected because the
  command can expose raw provider configuration, including credentials. Model
  discovery must use the ACP `session/new` response instead.
- Treating every Kimi `session/request_permission` callback as a tool approval was
  rejected because Kimi also uses that callback for questions and plan review.
- Building a Kimi-specific transport was rejected because the existing shared ACP
  runtime already covers Kimi's stdio lifecycle and content capabilities.

## Terminal-first fork — 2026-09-06

- Plan 003 is a standalone handoff for `feature/terminal-first-workspace`, based on `30c0c7cadb824a959c553b9b25834be4b41aa7bc`. It does not depend on the Kimi plans or authorize replaying them.
- Execute its six phases in order: materialization barrier → idempotent CLI launch → durable main-terminal binding → tabs and lifecycle → disabled future Chat control → integration and documentation.
- The target clone was checked out on `personal` during planning. Read plan 003 before choosing a checkout; preserve unrelated working-tree edits.
- All plans remain local handoff artifacts. Do not commit them automatically.
